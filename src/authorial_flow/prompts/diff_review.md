Review the actual diff after deterministic protection/tests. Reject protected-data mutation,
source-specific hardcoding, unrelated changes, checkpoint/credential regressions, authority weakening,
or detector-only degradation. Confirm the diff actually implements the approved causal repair.
