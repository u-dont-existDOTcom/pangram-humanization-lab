# Twenty-trial Railway continuation recovery — 2026-09-19

**Result: no detector-passing continuation recovered.** Twenty text-generation results, ten fresh returned threads and ten follow-ups. One rejected incorrect-project dispatch is separate from the twenty generations. The sole selected standalone paragraph passed provisional same-supervisor editorial/preservation review, then returned Pangram 4.0 AI 1.0 / Human 0.0 with exact UTF-8 History binding. No repeat or API-credit call.

Historical provider correction: September 15 success was Railway Agent per Joel, not GPT-5.6 Pro. Exact underlying model, old request bytes, and thread reuse were not recovered. Episode 005 preserves a 68-word prefix, 51-word source, and 98-word successful replacement; its 166-word combined boundary was owner-reported Human. September 19 corrected attempts provided 137-word Human P1 plus four protected conditions and then a compact prose-source control, which generated ordered paraphrases. Those are not exact replays of the older task.

Controls now observed: rewriting the old source, a positive example plus thread reuse, changing projects, and shortening the continuation unit did not reproduce success. Conversational answers sometimes sounded less schematic than their article conversions, but the final candidate still failed the detector. That strategy is not validated. Trial 6 returned an off-topic editing paragraph from an old-topic-only prompt; the cause of that mismatch is unknown, so it is not a valid clean-context control.

All exact messages and outputs are preserved in `trials_public.json`; shared thread labels and parent_trial fields preserve interaction order without publishing provider identifiers. `PRE-DETECTOR-AUDIT.md` is the premeasurement provisional verdict; do not overwrite it with hindsight. `OUTCOME.json` records the negative result. `selected_paragraph.txt` is a rejected experimental endpoint, not article-final copy. The GUI receipt remains in the content-addressed state/gui-runs directory.

No article master, owner P1/P2, or Railway infrastructure was edited. The requested twenty-trial batch is complete, but the humanization owner outcome remains OPEN / UNMET. A genuinely historical causal comparison still requires the exact September 15 messages/context, or an explicitly labeled reconstruction rather than a claimed exact replay.
