# Romance Vows — R4 final two-call preregistration

**Date:** 2026-08-16  
**Mode:** detector-rule research; no article authority  
**Owner rule-learning allowance before this batch:** 18 / 20 new calls used

## Result that determines the final design

On the frozen R3 old-assistant backbone (owner Bible, old final three-move conclusion removed):

- baseline: AI `0.2430769205`;
- C only, remove `Those origins still matter.`: AI `0.3190831244`;
- D only, replace the abstract old murky-middle realization with Joel's concrete behavioral realization: AI `0.2723928690`;
- E only, add the Tamera provenance parenthetical: AI `0.2551965117`;
- D+E together: AI `0.1756253392`.

D and E therefore show a strong interaction: each is neutral-to-worse alone, while together they materially improve the old tail. The D+E result makes the polyamory/murky portion Human, leaving the earlier chosen-exclusivity/history/property span as the remaining AI region.

The direct owner endpoint differs from D+E in two additional late-tail details:

- C: it omits the generic `Those origins still matter.` bridge;
- F: it uses Joel's `y'all are less likely...` in the disclosure sentence rather than the assistant's `people are less likely...`.

C was harmful alone. F is idiolect and must not be promoted as a humanization trick without ruling out the structural package first.

## Final two paid cells

### `TAIL_CDE_STRUCTURAL_PACKAGE`

Apply C + D + E while retaining `people are less likely...`.

Purpose: test whether the complete structural/provenance tail repair works without relying on the idiolect token.

### `TAIL_CDEF_EXACT_OWNER_LATE_TAIL`

Apply C + D + E and also restore `y'all are less likely...`, making the late tail from `I would rather make every attraction...` through the stopping point match Joel's direct owner realization.

Purpose: negative-control the possibility that the final flip depends on local idiolect/boundary segmentation rather than the structural package.

## Precommitted interpretation

- CDE materially improves or reaches Human, with little additional effect from F → promote a distributed tail-architecture interaction: provenance-bearing polyamory complication + behavioral murky realization + removal of generic historical aftercare work as a package even though components can have null/reversed main effects.
- CDE remains Mixed but CDEF reaches Human → do **not** conclude that `y'all` is a rule. Treat the residual as boundary/idiosyncrasy sensitivity and stop local decomposition; the useful lesson is that detector effects can require a natural owner realization as a whole and resist clean semantic factorization.
- both remain Mixed → the remaining difference depends on upstream context outside this late tail. Since R1 showed assistant middle clusters are null on the owner backbone, this would be strong evidence of higher-order backbone interaction rather than a local repair rule.
- both worsen → same conclusion, stronger: local factor signs are not stable enough for further Vows rule extraction.

These are calls 19–20 of Joel's current 20-call owner-authorized learning allowance. After they complete, stop paid rule-learning and ask before any additional calls, even if more hypotheses remain.