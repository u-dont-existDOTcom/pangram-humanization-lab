Joel Articles runtime routing summary

Use the full Joel Articles policy files in Project Sources as authority; do not treat this compact file as a replacement for MASTER-INSTRUCTIONS.md, TASK-MODES.md, HUMANIZATION-AND-COHERENCE.md, EDIT-CONTRACT-AND-LEDGERS.md, or the active project state.

For Joel's byline, preserve human authority rather than automatically preserving every proposition in an AI-generated draft. Classify source provenance and task mode first. Owner-final/locked material remains protected. In P2S, substantive propositions and certainty are locked. In P3/P4, AI-selected claims, citations, causal bridges, order, headings, and stopping points may be repaired, expanded, researched, or rejected when authorized by the task and policy.

The blocking order for detector-sensitive work is semantic sanity -> recover/choose architecture -> curious-reader thought flow -> draft -> completed-output cold audits -> fidelity/coherence/editorial gates -> detector last. Generate from the live pressure of the thought rather than a predicted completed paragraph. If semantic sanity fails, escalate to developmental/research repair and return to thought-flow after the conceptual substrate is repaired.

Human judgments are authority for their scoped case. Record them as learning data, but promote a cross-project rule only after repeated analogous support, held-out success, or explicit owner confirmation. Model-derived source-order positives are diagnostic only. Personal facts, beliefs, memories, relationships, and article-specific positions never become style rules.

Default humanize behavior: diagnose provenance and mode automatically. Produce one recommended candidate when there is a clear winner; show 2-3 only for materially different thought routes or unresolved authorial choices. In substantive mode, distinguish conservative reconstruction, developmental reconstruction, and research-informed alternatives when relevant. Research automatically only when a factual/source uncertainty could materially change the thought; prefer primary sources and keep source support separate from Joel's inference or position.

Freeze the editorial winner before Pangram. Pangram is downstream diagnostic/filter evidence, never the arbiter of thought quality. Try bounded meaning-preserving detector variants. If the strongest prose does not pass, preserve and surface it alongside the best equally valid passing alternative rather than silently substituting worse prose.

Interrupt Joel only for irreducible authorial judgment or unavoidable credentials/permissions. Machine failures, retries, logs, provider fallbacks, regression diagnosis, packaging, and repair are machine work. For flow rejection, ask for the first bad edge and whether the prior move was already the natural stop; keep local-edge and global precomputed-outline judgments as distinct learning targets.

Use local paths under ~/Téléchargements in instructions to Joel. Normal runtime must checkpoint/resume, keep a visible heartbeat during silent provider calls, isolate secrets from child agents, preserve evidence automatically, and never require Joel to ferry intermediate logs or ZIPs.
