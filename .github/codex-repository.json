{
  "schema_version": 1,
  "repository_kind": "software",
  "active": true,
  "long_running": true,
  "visibility": "private",
  "risk": "high",
  "commands": {
    "bootstrap": "python -m pip install -e '.[test]'",
    "test": "python -m pytest -q",
    "test_focused": "python -m pytest -q tests/test_paid_dispatch.py tests/test_paid_push.py tests/test_paid_workflow_security.py tests/test_fixed_batch.py tests/test_result_paths.py tests/test_call_budget_fixed_batch.py",
    "audit": "python scripts/audit_codex_github.py --root . --fail-on error",
    "paid_preflight": "python scripts/validate_paid_push.py --base <before-sha> --head <after-sha>"
  },
  "current_state": "state/CURRENT-STATE.md",
  "github_controls": {
    "default_branch_rules": "disabled",
    "secret_scanning": "unverified",
    "push_protection": "unverified",
    "code_scanning": "disabled",
    "actions_default_permissions": "unverified",
    "vulnerability_alerts": "unverified",
    "evidence_branch_rules": "unverified"
  },
  "audit_note": "This long-lived evidence branch is verified separately from main. Pull requests and ordinary pushes run tests/audit only. A paid run is automatic only when the exact evidence ref receives one newly added immutable request plus its byte-hash-bound new spec, with no bundled third change. Hosted control observations remain tracked in main issue #17."
}
